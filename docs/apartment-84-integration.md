# Квартира ID 84 — отчёт интеграции

## Исправление полного видео и галереи — 23 сентября 2026

Этот раздел заменяет прежние выводы ниже о коротком видео, главной фотографии и постере. Старый отчёт оставлен как история первоначальной интеграции.

- Новый исходник `WhatsApp Video 2026-09-23 at 09.58.30.mp4` переименован в `public/apartments/albisoara-16-84/video/apartment-tour.mp4`. Перекодирования и обрезки не было.
- Итог: **257,32 секунды (4:17,32), 26 469 029 байт**, MP4, H.264 Baseline / yuv420p, AAC LC / stereo / 44100 Hz. Записанное разрешение 1024×576; матрица поворота −90° даёт отображение 576×1024. Пропорции плеера соответствуют отображению. FFmpeg показывает 24 fps average / 25 tbr; исходная частота кадров сохранена.
- SHA-256 исходника и конечного файла совпадают: `5BE22DFA042E3EE0AF1B9DAE3E98F958DA39A4A69C8D9314CC73A35C14B3426E`. Полные 4 минуты 17 секунд сохранены байт в байт.
- `8.webp` — существующее фото спальни с двуспальной кроватью — первое в галерее, главное фото, карточка, OG и poster видео. Описания фото на RU/RO/EN/UK/CS переставлены в соответствующий порядок; OG-размеры 960×1280.
- Галерея ID 84: после закрытия lightbox остаётся выбранный кадр; окно из четырёх миниатюр следует за текущим индексом; свайп учитывает вертикальное смещение и идентификатор указателя; жесты на кнопках не вызывают дополнительное переключение; подавляется клик после свайпа и перетаскивание браузером изображения. Новое поведение включено только для ID 84.
- Chrome desktop 1440×1000 и мобильная эмуляция 390×844: проверены полный цикл 18 фотографий без broken images, Next/Previous, миниатюры, touch/swipe в обе стороны, переход через границу списка, вертикальная прокрутка без переключения, открытие/закрытие lightbox и сохранение индекса. Проверка на физическом телефоне не выполнялась.
- Видео: проверены начало, 128-я секунда, 256-я секунда и событие окончания, Play/Pause, seek, volume/mute, fullscreen и poster. HTTP Range в конце файла возвращает 206. До запуска браузер не запрашивает MP4, readyState=0, preload=none; controls и playsInline включены, autoplay отсутствует. Заголовок проверен на пяти языках.
- Весь видеофайл и аудиодорожка декодированы без ошибок. Аудио содержит сигнал (mean −33,3 dB, max 0 dB); прослушивание человеком не выполнялось.
- Финальные `npm run type-check`, `npm run lint`, `npm run build` прошли. Изменения проверены локально; публикация не выполнялась.
- Повторная браузерная проверка: `node scripts/verify-apartment-84-media.mjs` при работающем production-сервере. Требуется установленный Playwright и Chrome; при необходимости `PLAYWRIGHT_MODULE` задаёт путь к `playwright/index.mjs`, `CHROME_PATH` — к браузеру. Другой origin передаётся первым аргументом. Тест ограничен квартирой ID 84.

Дата проверки: 23 сентября 2026. Изменения локальные; публикация на Vercel не выполнялась.

1. Изменены `.gitignore`, `app/sitemap.ts`, `components/ApartmentDetails.tsx`, `components/ApartmentGallery.tsx`, `components/ResponsiveImage.tsx`, `lib/apartmentDistricts.ts`, `lib/apartmentLocalization.ts`, `lib/apartments.ts`, `lib/newApartmentLocalization.ts`, `lib/seo.ts`. Добавлены `.vercelignore`, `scripts/verify-apartment-84.mjs`, этот отчёт, 18 WebP, MP4 и обложка видео в `public/apartments/albisoara-16-84/`.
2. ID 84 добавлен в существующий массив `apartments`. Каталог, поиск ID, Premium, фильтры, подборка центра и детали используют общий источник. Цена 1200 MDL/сутки, две отдельные комнаты (существующий тип `1+1` / `oneBedroom`), до 4 гостей. Неподтверждённые этаж, подъезд и количество кроватей оставлены null.
3. Canonical: https://rentplace.md/apartment/albisoara-16-84. Локальный просмотр: http://localhost:3000/apartment/albisoara-16-84.
4. RU, RO, EN, UA, CZ интегрированы. В проекте украинский и чешский имеют стандартные коды `uk` и `cs`. Подготовлены тексты, характеристики, title, description и 18 содержательных alt на каждом языке. Расстояния и время в пути не придуманы.
5. Существующая серверная SEO-система формирует уникальные title/description, canonical, hreflang, Open Graph, Twitter, Apartment/LodgingBusiness и BreadcrumbList. Проверены 1200 MDL, две комнаты, вместимость 4, 18 реальных изображений и одна сущность квартиры. Основное изображение — горизонтальная гостиная `5.webp`; MIME и размеры 1280×960 заданы корректно.
6. Sitemap автоматически содержит новую квартиру, все 18 image:loc и языковые alternates. Дата новой записи — 2026-09-23. Robots разрешает индексацию, noindex отсутствует. Русский canonical без query, остальные версии сохраняют принятую в проекте схему `?lang=ro/en/uk/cs`.
7. Подключено 18 фотографий, каждая доступна в существующей галерее. Пройдены все 18 переходов в браузере; alt различаются и соответствуют изображениям.
8. Исходный вес JPEG: 1 856 637 байт (1,86 МБ / 1,77 МиБ).
9. Итоговый вес фотографий: 1 488 514 байт (1,49 МБ / 1,42 МиБ).
10. Экономия: 368 123 байта, 19,83%. У исходников уже умеренное сжатие, поэтому агрессивное дальнейшее ухудшение качества не применялось. Вариант WebP quality 86 давал лишь 3,42% экономии; выбран quality 82, проверен визуально.
11. JPEG преобразованы в WebP через sharp, quality 82, effort 6. Разрешение сохранено: 960×1280 у 17 вертикальных фото и 1280×960 у горизонтального. Максимальная сторона 1280 px. Исходники сохранены в `.local-media/albisoara-16-84/`, исключённой из Git и Vercel.
12. Для новых фотографий включён штатный оптимизатор Next.js с responsive sizes/srcset. Существующая глобальная конфигурация форматов AVIF/WebP и кэша не изменялась. Одна preload-картинка; четыре lazy-миниатюры; остальные снимки не монтируются до выбора. Одинаковое качество 82 у галереи и lightbox уменьшает число вариантов кэша. Новых runtime-зависимостей нет; FFmpeg используется только локально и исключён из deployment.
13. Использована существующая ApartmentGallery: главное фото, четыре миниатюры, навигация по всем 18, полноэкранный режим. Геометрия фото зарезервирована aspect-ratio. Проверены мобильная ширина 390 и desktop 1440, открытие/переключение/закрытие, отсутствие горизонтального переполнения.
14. Предоставленный `apartment-tour.mp4.MOV` фактически является Live Photo балкона длительностью 2,635 секунды, а не полным обходом квартиры. Подключён именно этот реальный ролик в локализованном разделе. MOV/HEVC с несколькими служебными дорожками преобразован в MP4 H.264/yuv420p + AAC, 720×960, CRF 22, preset slow, audio 96 kbit/s, faststart. Служебные дорожки и метаданные удалены. Обложка 42 000 байт извлечена из реального кадра. Плеер использует controls, playsInline, preload=none; autoplay отсутствует. Существующие city-video карточки предназначены для внешних YouTube/TikTok-публикаций и требуют иных метаданных, поэтому для локального небольшого файла использован стандартный video внутри существующей страницы. VideoObject не создавался: дата публикации не подтверждена, дата съёмки её не заменяет.
15. Видео до: 4 248 842 байта; после: 529 761 байт. Уменьшение 87,53%. Исходный MOV сохранён в `.local-media/albisoara-16-84/` и не публикуется.
16. Видео добавляет около 0,53 МБ передачи на полный просмотр, плюс 42 КБ обложки. При первоначальной загрузке браузер показал readyState=0; после ручного запуска — readyState=4, размер 720×960, воспроизведение дошло до конца. HTTP Range отвечает 206. При текущем размере внешний сервис необязателен; при массовых просмотрах расход растёт пропорционально. Для будущих больших обзоров стоит отдельно оценить CDN/видеохостинг с учётом реального трафика.
17. `npm run type-check`: успешно.
18. `npm run lint`: успешно, без ошибок и предупреждений.
19. `npm run build`: успешно; сгенерировано 90 статических страниц/служебных маршрутов. Динамическая маршрутизация квартир сохранена.
20. Обнаружена существующая ошибка галереи: pointer capture на родительском блоке перенаправлял клики с кнопок, из-за чего открытие и переключение мышью не срабатывало. В промежуточной работе исправлены синтаксис и временные файлы, мешавшие lint.
21. Убрана захватывающая клики строка в общем обработчике галереи; сохранён обработчик свайпов. В браузере проверены открытие lightbox, 18 последовательных переходов и закрытие. Broken assets, duplicate ID/URL и ошибки метаданных в финальных проверках не выявлены.
22. Все 37 существующих тестов прошли. Осталось прежнее предупреждение Node MODULE_TYPELESS_PACKAGE_JSON в тестах TikTok. Дополнительный production smoke-test `node scripts/verify-apartment-84.mjs` проверил пять языков, JSON-LD, canonical/hreflang, sitemap, каталог/Premium/центр, 18 файлов, оптимизатор изображений, видеоплеер и все 47 страниц квартир. На локальной сборке HTML страницы с gzip — 22,6–23,9 КБ; главное WebP после оптимизатора: 32 948 байт при w=640 и 68 082 при w=1200, миниатюра w=256 — 7 780 байт. Это не полный вес загрузки страницы: общие JS/CSS и прочие изображения здесь не учтены. Полевые LCP/CLS и расход Vercel после публикации не измерялись, поэтому численные оценки не заявляются. Фото и видео имеют зарезервированные пропорции, LCP-фото получает единственный preload. Полного видеообзора квартиры не предоставлено.
23. Следующий этап отдельного аудита: проверить полевые Core Web Vitals и фактический Vercel usage после публикации; оценить существующее глобальное unoptimized для старых квартир, размер общих клиентских данных и дубли признаков в интерфейсе. Текущая задача не меняет эти части глобально. При необходимости заменить короткий Live Photo настоящим обзором, сохранив отложенную загрузку и измерив вес нового файла.

## Повторная проверка

Запустить production-сборку и локальный сервер, затем `node scripts/verify-apartment-84.mjs`. Другой адрес сервера можно передать первым аргументом. Проверка читает страницы и ассеты, не меняя данные и бронирования.
